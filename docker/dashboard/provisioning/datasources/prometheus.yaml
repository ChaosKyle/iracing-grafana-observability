apiVersion: 1

datasources:
  - name: Prometheus-iRacing
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: "15s"
      httpMethod: "GET"
    editable: true